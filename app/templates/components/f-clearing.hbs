{{#each image in images}}
  <li {{bind-attr class="image.featured:clearing-featured-img"}}>
    <a class="th" {{bind-attr href=image.url}}>
      <img {{bind-attr alt=image.alt data-caption=image.caption src=image.thumbnail}}>
    </a>
  </li>
{{/each}}
